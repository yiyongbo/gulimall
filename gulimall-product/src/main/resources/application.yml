server:
  port: 10000

spring:
  application:
    name: gulimall-product
  datasource:
    username: root
    password: PGzuWU8E
    url: jdbc:mysql://211.159.224.38:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 60000
      validation-timeout: 3000
      idle-timeout: 600000
      login-timeout: 5
      max-lifetime: 1800000
      maximum-pool-size: 10
      minimum-idle: 5
      read-only: false
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  thymeleaf:
    cache: false
  redis:
    host: 211.159.224.38
    port: 6379
    password: 5qzzo97C
  cache:
    type: redis
    redis:
      time-to-live: 3600000
      use-key-prefix: true
      # 是否缓存空值，防止缓存穿透
      cache-null-values: true

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
logging:
  level:
    com.yee.gulimall: error

gulimall:
  thread:
    coreSize: 20
    maxSize: 200
    keepAliveTime: 10

